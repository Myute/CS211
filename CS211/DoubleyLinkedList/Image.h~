#ifndef IMAGE_H
#define IMAGE_H

#include<string>

using namespace std;

class Image
{
 private:
  string name;
  double size;
  string format;
  string resolution;
 public:
  Image* next;
  Image* prev;
  Image(string n, double s, string f, string r);
};

#endif
