#include <iostream>

using namespace std;

int sum(int);

int main()
{

  int input;
  cin >> input;
  cout << sum(input) << endl;

  return 0;
}

int sum(int val)
{
  int sum = 0;
  
  while(val != 0)
    {
      sum+=(val%10);
      val/=10;
    }
  return sum;
}
